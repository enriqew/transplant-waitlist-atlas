name: transplant_waitlist_atlas
version: "0.1.0"
config-version: 2

profile: transplant_waitlist_atlas

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"
  - "logs"

models:
  transplant_waitlist_atlas:
    staging:
      +schema: silver
      +materialized: view
    marts:
      +schema: gold
      +materialized: table
